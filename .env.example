# Environment Variables for Luna
# Copy this file to .env and fill in your values
# These are used during development and build processes

# SENTRY - Error Tracking & Performance Monitoring
# =============================================================================
# Get your DSN from https://sentry.io -> Project Settings -> Client Keys
# 
# IMPORTANT: 
# - In DEVELOPMENT: Sentry is DISABLED automatically (no need to set this)
# - In PRODUCTION: Set these in GitHub Secrets for CI/CD builds
#
# For GitHub Actions, add these as repository secrets:
# - SENTRY_DSN: Your Sentry DSN (for both main and renderer)
# - SENTRY_AUTH_TOKEN: Used for uploading source maps (Internal Integration token)
# - SENTRY_ORG: Your Sentry Organization slug
# - SENTRY_PROJECT: Your Sentry Project slug
#
# Main process (Electron) - set in shell environment or CI/CD
SENTRY_DSN=

# Renderer process (Vite/React) - must be prefixed with VITE_
VITE_SENTRY_DSN=

# Sentry Organization, Project and Auth Token (for source maps upload)
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# App version for release tracking (automatically set in CI/CD)
VITE_APP_VERSION=

# =============================================================================
# DEVELOPMENT FLAGS
# =============================================================================
# Set to 'true' to enable development mode features
# DEV=true

# Vite dev server URL (set automatically by Vite)
# VITE_DEV_SERVER_URL=http://localhost:5173

# =============================================================================
# GITHUB SECRETS SETUP INSTRUCTIONS
# =============================================================================
# 1. Go to your GitHub repository
# 2. Navigate to Settings > Secrets and variables > Actions
# 3. Click "New repository secret"
# 4. Add the following secrets:
#
#    Name: SENTRY_DSN
#    Value: https://d05b1c6c0aab3736deaee734a9b06a4a@o4508971823464448.ingest.us.sentry.io/4510704746627072
#
# These secrets will be automatically injected during GitHub Actions builds.
